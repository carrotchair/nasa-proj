// import { meteorQuerySchema } from "../schemas/meteorSchema.js";

// function validateMeteorQuerySchema(query) {
//   const { error } = meteorQuerySchema.validate(query);
//   if (error) {
//     return res.status(400).json({ error: error.details[0].message });
//   }
// };

// export default validateMeteorQuerySchema;